# Package Checks with devtools

## Overview
Use `devtools` to run checks and ensure package quality.

## Setup

Install `devtools`:

```r
install.packages("devtools")
```

## Usage

Run package check:

```r
devtools::check()
```

## GitHub Copilot Prompt

> "Run a package check using `devtools`."
